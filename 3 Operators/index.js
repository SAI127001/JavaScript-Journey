// arithemetic operator = operands (values, variables, etc.)
//   operator(+, -, /, %, *)

let students = 60;

let extraStudents = students * 6;

console.log(`No of Extra Students ${extraStudents}`);
